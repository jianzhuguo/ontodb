apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ontodb.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ontodb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['--timeout=10', '{{ include "ontodb.fullname" . }}:{{ .Values.service.http.port }}/api/health']
  restartPolicy: Never
