# OntoDB 阶段五计划：生产就绪 + 性能优化 + 功能增强

> 目标：从"能用"到"好用"，提升生产可靠性、查询性能和功能完整性
> 预计工时：2-3 周

---

## 一、生产就绪 (Production Readiness)

### P5.1 存储引擎 WAL fsync 策略可配置
- **现状**: `sync_wal_on_commit` 只有 true/false 两档
- **目标**: 支持 `always` / `batch(N)` / `off` 三种模式
- **收益**: 用户可按 durability 需求灵活配置

### P5.2 引擎启动时数据完整性校验
- **现状**: 启动时只做 WAL replay，不校验 SSTable 完整性
- **目标**: 启动时可选校验 SSTable checksum + bloom filter 一致性
- **收益**: 提前发现静默数据损坏

### P5.3 优雅关闭 (Graceful Shutdown) 完善
- **现状**: Ctrl+C 后可能丢失未 flush 的 MemTable
- **目标**: 捕获 SIGTERM/SIGINT，flush MemTable + sync WAL 后退出
- **收益**: 防止数据丢失

### P5.4 存储空间监控与告警
- **现状**: 无磁盘空间监控
- **目标**: 暴露 `disk_usage_bytes` / `memtable_size_bytes` 指标，低空间时拒绝写入
- **收益**: 防止磁盘写满导致崩溃

### P5.5 连接池与背压控制
- **现状**: TCP/HTTP 连接数有上限但无队列
- **目标**: 实现请求队列 + 超时机制 + 503 响应
- **收益**: 高负载下保持可用

---

## 二、查询性能优化 (Query Performance)

### P5.6 统计信息收集与基数估计
- **现状**: CostModel 使用硬编码选择率 (10%)
- **目标**: 收集表行数、列 NDV、直方图，用于基数估计
- **收益**: 查询计划更准确，避免全表扫描

### P5.7 查询计划缓存优化
- **现状**: PlanCache 使用简单 LRU
- **目标**: 支持参数化查询缓存，避免重复解析
- **收益**: OLTP 场景下减少 CPU 开销

### P5.8 并行查询执行
- **现状**: 单线程执行查询
- **目标**: 支持并行扫描 (分区表) 和并行聚合
- **收益**: 分析查询性能提升

### P5.9 索引覆盖扫描 (Index-Only Scan)
- **现状**: 索引查找后必须回表取数据
- **目标**: 如果查询列都在索引中，直接从索引返回
- **收益**: 减少 I/O，提升点查性能

### P5.10 向量搜索性能优化
- **现状**: HNSW 搜索使用本地 HashSet
- **目标**: 支持 SIMD 加速距离计算，批量搜索优化
- **收益**: 向量搜索吞吐量提升 2-3x

---

## 三、功能增强 (Feature Enhancement)

### P5.11 事务隔离级别增强
- **现状**: 只有 Snapshot Isolation
- **目标**: 支持 Read Committed / Repeatable Read
- **收益**: 兼容更多应用场景

### P5.12 批量导入/导出
- **现状**: 逐条 INSERT
- **目标**: 支持 CSV/JSON 批量导入，COPY 命令
- **收益**: 数据加载速度提升 10x+

### P5.13 Change Data Capture (CDC)
- **现状**: 无变更捕获
- **目标**: 基于 WAL 的变更流，支持 Kafka/Pulsar 输出
- **收益**: 支持实时数据同步

### P5.14 全文搜索
- **现状**: 只有精确匹配和 LIKE
- **目标**: 基于倒排索引的全文搜索，支持中文分词
- **收益**: 支持语义搜索场景

### P5.15 图查询增强 (Gremlin/GraphQL)
- **现状**: 只有简单 BFS/DFS
- **目标**: 支持 Gremlin 遍历语言或 GraphQL 查询
- **收益**: 图数据库标准兼容

---

## 四、测试与可靠性 (Testing & Reliability)

### P5.16 集成测试套件
- **现状**: 只有单元测试和少量 E2E 测试
- **目标**: 完整的集成测试覆盖所有 API 端点
- **收益**: 防止回归

### P5.17 模糊测试 (Fuzzing)
- **现状**: 有少量 fuzz 测试
- **目标**: 对 parser、WAL、SSTable 进行持续 fuzz
- **收益**: 发现边界 bug

### P5.18 混沌测试
- **现状**: 无
- **目标**: 模拟崩溃、网络分区、磁盘满等故障
- **收益**: 验证系统容错能力

---

## 五、文档与可观测性 (Docs & Observability)

### P5.19 架构文档
- **现状**: 无系统架构文档
- **目标**: 存储引擎、查询引擎、分布式架构文档
- **收益**: 降低新人上手成本

### P5.20 性能调优指南
- **现状**: 无
- **目标**: 参数调优、监控指标解读、常见问题排查
- **收益**: 用户自助优化

---

## 优先级排序

| 优先级 | 编号 | 任务 | 预计工时 |
|--------|------|------|----------|
| P0 | P5.3 | 优雅关闭 | 0.5天 |
| P0 | P5.4 | 存储空间监控 | 0.5天 |
| P1 | P5.6 | 统计信息收集 | 2天 |
| P1 | P5.1 | WAL fsync 策略 | 0.5天 |
| P1 | P5.2 | 启动完整性校验 | 1天 |
| P1 | P5.12 | 批量导入 | 2天 |
| P2 | P5.5 | 连接池背压 | 1天 |
| P2 | P5.7 | 查询计划缓存 | 1天 |
| P2 | P5.9 | 索引覆盖扫描 | 1.5天 |
| P2 | P5.10 | 向量 SIMD 优化 | 2天 |
| P3 | P5.16 | 集成测试套件 | 2天 |
| P3 | P5.17 | 模糊测试 | 1天 |
| P3 | P5.19 | 架构文档 | 1天 |

---

## 建议执行顺序

1. **第一周**: P5.3 → P5.4 → P5.1 → P5.2 → P5.12 (生产就绪 + 批量导入)
2. **第二周**: P5.6 → P5.7 → P5.9 → P5.5 (查询性能优化)
3. **第三周**: P5.10 → P5.16 → P5.17 → P5.19 (向量优化 + 测试 + 文档)
