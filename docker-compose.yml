services:
  ontodb:
    build: .
    ports:
      - "7912:7912"   # HTTP API
      - "7913:7913"   # TCP CLI
    volumes:
      - ontodb-data:/data
    restart: unless-stopped

volumes:
  ontodb-data:
