# OntoDB — 本体内核嵌入的多模态语义数据库

**[English](README.en.md)** | 中文

**全球首个将 OWL 推理引擎嵌入数据库内核的多模态语义数据库**

支持 12 种数据类型统一存储，一次 INSERT 自动完成 7 步写入联动，嵌入式增量推理性能较外置方案提升 100 倍。

---

## 版本对比

| 功能 | 社区版（BUSL-1.1） | 企业版（商业许可） |
|------|-------------------|------------------|
| **存储引擎** | ✅ LSM-Tree + MVCC + WAL | ✅ 同社区版 |
| **12 种数据类型统一存储** | ✅ | ✅ |
| **SQL/OntoQL/SPARQL 查询** | ✅ | ✅ |
| **HNSW 向量索引** | ✅ | ✅ |
| **OWL 2 RL 推理** | ✅ 增量不动点+推导追溯 | ✅ 同社区版 |
| **七步写入联动** | ✅ | ✅ |
| **PostgreSQL/MySQL 兼容** | ✅ | ✅ |
| **TLS/mTLS 安全传输** | ✅ | ✅ |
| **审计完整性链（SHA256）** | ✅ | ✅ |
| **CDC 变更数据捕获** | ✅ | ✅ |
| **数据分片** | ✅ 类/范围/哈希 | ✅ 企业级+跨分片查询 |
| **Raft 分布式复制** | ✅ | ✅ |
| **基本规则 API** | ✅ DSL 解析+CRUD+热更新 | ✅ |
| **边缘设备支持** | ✅ | ✅ |
| **插件框架** | ✅ | ✅ |
| 高级推理引擎 | ❌ | ✅ 完整 DSL+OWL 集成+分布式推理+性能分析 |
| 集群自动故障转移 | ❌ | ✅ |
| 跨分片查询 | ❌ | ✅ |
| 慢查询监控 | ❌ | ✅ 可观测性 |
| 全量备份 | ❌ | ✅ |
| RBAC 三权分立 | ❌ | ✅ |
| LDAP/SAML 集成 | ❌ | ✅ |
| 数据脱敏 | ❌ | ✅ |
| SM4/AES 加密 | ❌ | ✅ |
| KMS 密钥管理 | ❌ | ✅ |
| 审计日志轮转+保留 | ❌ | ✅ |
| CRC 数据校验 | ❌ | ✅ |
| 滚动升级 | ❌ | ✅ |

---

## 为什么选择 OntoDB

| 维度 | 传统方案 | OntoDB |
|------|----------|--------|
| **架构** | MySQL + Neo4j + Milvus + InfluxDB + PostGIS + Jena，6 个系统 | **1 个系统**，12 种数据类型统一存储 |
| **语义** | 数据写入后需要 ETL 流程生成语义，延迟秒级到分钟级 | **写入即语义就绪**，7 步联动管线原子完成，延迟微秒级 |
| **推理** | 推理引擎外置，跨系统数据搬运 | **OWL 推理内嵌存储引擎**，共享内存空间，零搬运 |
| **查询** | SQL + Cypher + SPARQL + 专用 API，4 种语言 | **OntoQL**，单一语法融合关系+图+向量+推理 |
| **部署** | 6 套独立系统，运维复杂 | **边缘三级部署**，从 ESP32 到云服务器统一架构 |
| **多租户** | 应用层加 tenant_id，容易遗漏 | **命名空间内核隔离**，键前缀路由零开销 |
| **数据生命** | TTL 固定过期，不懂数据价值 | **活数据衰减**，数据越用越活，不用自动降级 |
| **成本** | 6 套系统 × 6 份运维 × 6 份许可 | **1 套系统**，运维成本降低 80%+ |

---

## 12 种数据类型

OntoDB 在单一存储引擎中融合 12 种数据类型：

| 类别 | 数据类型 | 说明 |
|------|----------|------|
| **基础** | 键值对 | 高性能读写 |
| | 文档 | JSON/BSON，嵌套结构 |
| | 文本 | 倒排索引+中英文分词+BM25排序 |
| | 多媒体 | 元数据注册+SHA256内容哈希+类型/属性检索 |
| **高级** | 结构化记录 | 关系型数据+事务语义 |
| | 图结构 | 实体关系网络+遍历 |
| | 向量索引 | HNSW 语义相似性检索 |
| | 时序数据 | 传感器/监控/日志 |
| | 空间数据 | R-Tree+GeoHash 地理信息 |
| | 语义三元组 | 知识图谱 SPO |
| **实时** | 流数据 | CDC 变更捕获+连续查询 |
| | 事件数据 | 复杂事件处理 |

所有数据通过**统一语义锚点**（`{Class}::{PrimaryKey}`）关联，零映射表。

---

## 33 项核心技术

### 存储引擎

| # | 创新 | 一句话 |
|---|------|--------|
| 1 | **统一语义锚点** | `{Class}::PK` 同时作为存储键、图顶点、向量文档键、三元组主体，零映射表关联 |
| 2 | **七步写入联动** | 一次 INSERT 原子完成文档→图→三元组→推理→向量→索引，写入即语义就绪 |
| 3 | **全模态统一存储** | LSM-Tree + 键前缀路由，12 种数据类型在同一引擎中共存 |
| 4 | **二进制行零拷贝过滤** | 在二进制数据上直接执行过滤，无需反序列化，解析时间减少 60% |
| 5 | **三阶段锁提交** | 准备→预提交→提交，避免写锁与索引锁的嵌套死锁 |
| 6 | **四级缓存内核嵌入** | 块缓存+查询缓存+语义缓存+自适应内存管理，零网络开销，缓存与内核共享内存空间 |

### 推理引擎

| # | 创新 | 一句话 |
|---|------|--------|
| 7 | **嵌入式增量推理** | OWL 2 RL 推理内嵌存储引擎，增量不动点算法，性能提升 10-100 倍 |
| 8 | **推理安全机制** | 事实数预算控制防推理爆炸、并行推理线程池、循环继承检测 |
| 9 | **推导链路追溯** | 每条推导结果记录来源规则和输入事实，支持完整推理链查询 |

### 本体内核：OWL 2 RL 推理引擎

OntoDB 将 OWL 2 RL 推理引擎**嵌入存储引擎内核**，而非外挂组件。数据写入时自动推理，查询时自动展开子类，推导结果即时持久化。

```
传统方案：数据库 → ETL → 外挂推理引擎 → 推理结果写回数据库（延迟分钟级）
OntoDB：  数据写入 → 内核自动推理 → 推理结果即时持久化（延迟微秒级）
```

**支持的 OWL 2 RL 推理规则：**

| 规则 | 含义 | 示例 |
|------|------|------|
| **子类传播** | 子类实例自动属于父类 | 温度传感器 IS_A 传感器 → 查询"设备"自动包含 |
| **等价类** | 等价类实例互相包含 | 职工 = 员工 → 查询任一类自动包含另一类 |
| **传递性** | 传递关系自动推导 | A 包含 B，B 包含 C → A 包含 C |
| **对称性** | 对称关系双向推导 | A 互联 B → B 互联 A |
| **逆属性** | 逆关系自动推导 | A 是 B 的父类 → B 是 A 的子类 |
| **子属性** | 子属性自动继承父属性 | "管理"是"认识"的子属性 → 管理某人必认识某人 |
| **等价属性** | 等价属性互相替代 | "父亲" = "爸爸" → 查询任一自动包含另一类 |

**安全机制：** 推理预算控制（最大事实数防爆炸）、并行推理线程池、本体循环继承检测、推导链路完整追溯。

### 查询引擎

| # | 创新 | 一句话 |
|---|------|--------|
| 10 | **统一查询语言 OntoQL** | 单一语法融合 SQL + 图遍历 + 向量搜索 + 本体推理 + 时序 + 空间 |
| 11 | **SPARQL 支持** | 标准 SPARQL 1.1 查询，FILTER/OPTIONAL/UNION/EXISTS 全支持 |
| 12 | **推理子句** | 查询时实时推理，`SELECT * FROM Device` 自动包含所有子类实例 |
| 13 | **语义缓存** | 语义相似度查询自动命中缓存，不同文本相同语义视为同一查询 |
| 14 | **多模态融合查询优化** | 向量+关系/空间+时序/全文+向量跨模态查询自动选择最优执行计划 |
| 15 | **数据血缘追溯** | 内核级数据来源追踪，追溯到原始写入+每一步推理+规则触发 |
| 16 | **联邦查询** | 外部数据源（PG/MySQL/REST/CSV/JSON）映射为命名空间，统一查询 |

### OntoQL：统一查询语言

开发者只需学一种语言，就能操作所有数据模态。传统方案需要 SQL + Cypher + 专用向量 API + SPARQL 四种语言。

```sql
-- 关系查询
SELECT * FROM users WHERE age > 25;

-- 图遍历
GRAPH TRAVERSE FROM 'Person::1' OUT LABEL 'knows' DEPTH 3;

-- 向量搜索
VECTOR SEARCH ON documents (embedding) QUERY [0.1, 0.2] TOP 10;

-- 本体推理（自动展开子类）
SELECT * FROM Device;  -- 自动包含 Sensor, TemperatureSensor 等所有子类

-- 时空查询
SELECT * FROM pois WHERE ST_Distance(location, ST_Point(116.4, 39.9)) < 1000;

-- 上述模态可在同一查询中组合使用
```

### 活数据管理

| # | 创新 | 一句话 |
|---|------|--------|
| 17 | **活数据生命周期** | 指数/线性/对数三种衰减模型，三级半衰期(7h/70d/2yr)，访问自动激活 |
| 18 | **价值驱动查询** | 查询条件中使用价值得分过滤排序，高频数据自动提升优先级 |

### 活数据：数据价值自动衰减

传统数据库用 TTL 固定过期，OntoDB 的活数据让**数据像生物一样有生命**：不用就慢慢"死掉"，用一下就"复活"。

```
传统 TTL：创建时设 30 天过期 → 第 29 天还在用也删掉 → 被动等死
活数据：  频繁访问 → 价值上升 → 自动保留           → 主动续命
         长期不访问 → 价值衰减 → 自动降级/清理
```

| 特性 | 说明 |
|------|------|
| **三级半衰期** | 7 小时（高频热点）→ 70 天（中频温数据）→ 2 年（低频冷数据） |
| **三种衰减模型** | 指数衰减（默认）、线性衰减、对数衰减 |
| **自动激活** | 每次访问自动重置衰减时钟，累加价值得分 |
| **内核级集成** | 价值元数据与数据存储在同一记录中，原子性保证 |
| **向量索引集成** | 高价值数据的向量优先加载到内存，查询更快 |

### 时序与空间

| # | 创新 | 一句话 |
|---|------|--------|
| 19 | **TSM 列式存储** | 时间戳 Delta 编码 + 浮点值 Gorilla 编码，存储空间减少 60%+ |
| 20 | **时空联合索引** | 四叉树空间分区 + 时间线排序融合在单一数据结构中 |
| 21 | **嵌入式时空规则引擎** | STTRL 语言，地理围栏/超速/接近/异常位置等 10+ 种规则数据库层执行 |
| 22 | **三级分层存储** | 热层(内存)→温层(SSD)→冷层(HDD)，按数据温度自动迁移，查询透明 |
| 23 | **高级时序分析** | DTW 距离、流式异常检测、滚动/跳跃/会话窗口函数 |

### 分布式与扩展

| # | 创新 | 一句话 |
|---|------|--------|
| 24 | **数据分片** | 类/范围/哈希三种分片策略，运行时动态扩缩容和数据重平衡 |
| 25 | **Raft 共识** | 多节点集群一致性，持久化日志，集群白名单，配置同步 |
| 26 | **多协议接入** | PostgreSQL + MySQL + HTTP REST 三协议并存，现有应用无缝迁移 |
| 27 | **API 网关内嵌** | 负载均衡、限流、熔断、认证授权，无需独立部署 |
| 28 | **边缘三级部署** | MCU(<100KB) → 嵌入式 Linux → 边缘服务器，统一数据模型和 API |

### 安全与运维

| # | 创新 | 一句话 |
|---|------|--------|
| 29 | **CDC 变更捕获** | 内核级集成，WAL 实时提取变更事件，支持 Flink/Spark 消费 |
| 30 | **命名空间内核隔离** | `{namespace}::{class}::{pk}` 五层全栈隔离，键前缀路由零开销 |
| 31 | **等保2.0三权分立** | 系统管理员/安全管理员/审计管理员互斥，满足中国政务金融合规 |
| 32 | **数据脱敏引擎** | 内核级动态+静态脱敏，身份证号/手机号/银行卡号自动识别脱敏 |
| 33 | **CRC自动修复** | SSTable页级CRC校验，检测到损坏自动从Raft副本修复 |

### 命名空间：内核级多租户隔离

OntoDB 的命名空间不是外挂标签，而是**嵌入存储引擎内核**的全栈隔离机制。实体标识 `{namespace}::{class}::{pk}` 直接作为存储键，无需额外过滤字段。

```
传统方案：应用层加 tenant_id → 查询时 WHERE tenant_id = ? → 容易遗漏
OntoDB：  EntityId = {namespace}::{class}::{pk} → 键前缀路由 → 物理隔离，零开销
```

| 隔离层级 | 说明 |
|----------|------|
| **本体层** | 每个命名空间独立的类定义、属性约束、继承关系 |
| **数据层** | 键前缀 `{namespace}::` 物理隔离，无交叉 |
| **查询层** | 查询引擎自动应用命名空间上下文，无需 WHERE 过滤 |
| **索引层** | 向量/B-Tree/空间索引按命名空间隔离 |
| **推理层** | OWL 推理在命名空间范围内执行，不跨租户推导 |

一个 OntoDB 实例服务 N 个租户/项目，运维成本低，数据安全有保障。

---

## 性能基准

> **百万级吞吐，微秒级推理，100% 向量召回率。单一系统统一 12 种数据类型，替代 6 个独立数据库。**

### 存储引擎

| 指标 | OntoDB | SQLite | PostgreSQL | Redis |
|------|--------|--------|-----------|-------|
| 写入 | **1,126,486 ops/s** | ~50K | ~30K | ~100K |
| 读取 | **1,306,438 ops/s** | ~200K | ~100K | ~100K |

### 向量搜索

| 数据规模 | 召回率 | 延迟 |
|----------|--------|------|
| 10K | **100%** | **403µs** |
| 50K | **100%** | **1.03ms** |
| 200K | **99.7%** | **4.06ms** |

### OWL 推理（全球首创内嵌引擎）

| 操作 | OntoDB | 外挂引擎 |
|------|--------|---------|
| is_subclass_of | **390ns** | ~10ms |
| 单事实推理 | **50.8µs** | ~100ms |
| 批量推理（10K实体） | **22ms** | ~5s |

### 规则引擎

| 规模 | 耗时 |
|------|------|
| 10 条规则 | **34.5µs** |
| 1000 条规则 | **1.79ms** |

### 性能对标：OntoDB vs 各领域专用数据库

| 功能 | 专用数据库 | 专用数据库性能 | OntoDB | 说明 |
|------|-----------|--------------|--------|------|
| 关系存储 | PostgreSQL | ~30K ops/s | **1,126K ops/s** | 写入吞吐 |
| 向量搜索 | Milvus | ~1ms / 95-99% | **403µs / 100%** | 10K规模 |
| 图查询 | Neo4j | ~10ms | **<1ms** | BFS/DFS |
| OWL推理 | Apache Jena | ~100ms | **50.8µs** | 增量不动点 |
| 时序存储 | InfluxDB | ~200K ops/s | **1,126K ops/s** | TSM列式 |
| 规则引擎 | Drools | ~500µs/10条 | **34.5µs/10条** | DSL语法 |

> 详见 [BENCHMARK_REPORT.md](BENCHMARK_REPORT.md)

---

## 快速开始

### 从源码编译

```bash
# 前置要求：Rust 1.77+
git clone https://gitee.com/ontovalue/ontodb.git
cd ontodb
cargo build --release

# 启动服务器（PostgreSQL 协议 5432 + MySQL 协议 3306 + HTTP 7912）
./target/release/ontodb-server --data-dir ./data --http 0.0.0.0:7912
```

### Docker

```bash
docker compose up -d
```

### 用 OntoQL 创建第一个表

```sql
-- 定义本体
CREATE CLASS Device;
CREATE CLASS Sensor SUBCLASS OF Device;

-- 插入数据（自动完成 7 步联动）
INSERT INTO Sensor (id, location, temperature) 
VALUES ('T1', 'Factory_A', 36.5);

-- 查询时自动推理展开（Device 自动包含 Sensor 子类数据）
SELECT * FROM Device;

-- 图遍历
GRAPH TRAVERSE FROM 'Sensor::T1' OUT LABEL 'connects_to' DEPTH 3;

-- 向量搜索
VECTOR SEARCH ON documents (embedding) QUERY [0.1, 0.2] TOP 10;

-- 空间查询
SELECT * FROM devices WHERE ST_Distance(location, ST_Point(116.4, 39.9)) < 1000;

-- 时序查询
SELECT * FROM sensor_data WHERE time > '2024-01-01' LIMIT 100;
```

---

## 适用场景

| 场景 | 为什么用 OntoDB |
|------|----------------|
| **大模型 / AI 公司** | 向量+知识图谱+推理融合，RAG 检索增强+本体内置，不用外挂向量库+图库+推理引擎 |
| **互联网公司** | 12 种数据类型统一存储，一个系统替代 MySQL+Redis+Milvus+Elasticsearch+Neo4j |
| **SaaS 平台** | 命名空间内核级隔离，一个实例服务 N 个租户，零开销零泄露 |
| **知识图谱** | OWL 推理内嵌，写入即推理，推导链路可追溯，不用外挂推理引擎 |
| **智能搜索** | 向量+全文+语义推理融合查询，一个查询同时做语义搜索+知识推理 |
| **IoT / 工业互联网** | 时序+空间+图+本体推理统一，STTRL 时空规则引擎内置 |
| **数字孪生** | 12 种数据类型 + 活数据衰减 + 时空规则引擎 + CDC 实时事件 |
| **自动驾驶** | 边缘三级部署 + STTRL 时空规则 + CDC 实时事件 |
| **机器人** | 边缘低功耗 + 实时推理 + 时空感知 + 本体规则引擎 |
| **智慧医疗** | 命名空间多租户隔离+本体推理+时序数据（生命体征）+空间数据（病区定位） |
| **金融风控** | 本体规则引擎自动触发风控规则+实时流数据+CDC+审计链 |
| **电商推荐** | 向量相似性+图关系+活数据衰减（热度自动降级）+实时事件 |
| **智慧城市** | 空间+时序+图+本体推理，地理围栏+轨迹+设备关系统一管理 |
| **多项目数据中台** | 一个 OntoDB 实例跑 N 个项目，命名空间物理隔离互不干扰 |
| **边缘计算** | MCU < 100KB 内存到完整服务器，统一数据模型和 API |
| **具身智能** | 边缘推理 + 时空规则 + 本体驱动规则引擎 + 多体协同 |

---

## 项目结构

```
ontodb/
├── crates/
│   ├── onto-core/          # 核心类型（EntityId/BinaryRow/R-Tree/GeoHash/DTW）
│   ├── onto-storage/       # LSM-Tree 引擎（WAL/MVCC/HNSW/TSM/分层存储/Bloom）
│   ├── onto-ontology/      # OWL 2 RL 推理引擎（7 条规则/增量不动点/推导追溯）
│   ├── onto-query/         # 查询引擎（OntoQL/SPARQL/优化器/执行器/缓存）
│   ├── onto-graph/         # 图模型（邻接表/BFS/DFS 遍历）
│   ├── onto-server/        # 多协议服务器（PG/MySQL/HTTP/TLS/审计/CDC/限流）
│   ├── onto-cli/           # 命令行 REPL + dump/restore
│   ├── onto-plugin/        # 插件钩子管线（INSERT/UPDATE/DELETE 生命周期）
│   ├── onto-edge/          # 边缘设备 SDK（采集/地理围栏/上报）
│   ├── onto-sharding/      # 数据分片（类/范围/哈希策略）
│   ├── onto-raft/          # Raft 共识（持久化/集群白名单/配置同步）
│   └── ontodb-rules/       # 基本规则 API 服务（DSL 解析/CRUD/热更新）
├── sdk/                    # 多语言 SDK（Go/Python/TypeScript）
├── examples/               # 示例应用 + 规则模板（金融/工业/医疗）
├── frontend/               # 规则编辑器 Web UI
└── docs/                   # 文档
```

---

## 企业版功能特点

企业版在社区版基础上提供三大核心能力：

### 本体驱动规则引擎（全球首创）

**本体知识自动触发业务规则，领域专家只需定义本体，规则自动生效。** 不同于传统规则引擎需要人工编写每一条规则，OntoDB 企业版将 OWL 推理引擎与规则引擎深度融合，实现"知识即规则"。

| 能力 | 说明 |
|------|------|
| **本体→规则自动触发** | OWL 推理结果自动注入规则引擎，无需编码 |
| **企业级规则引擎** | 完整 DSL 语法、分布式推理、冲突检测、版本管理、性能分析 |

### 安全合规

满足政务、金融、医疗等行业合规要求。

| 能力 | 说明 |
|------|------|
| **身份认证** | RBAC 三权分立、LDAP/SAML 集成 |
| **数据保护** | 国密 SM4 + AES 加密、数据脱敏、密钥管理 |
| **审计合规** | 审计日志、合规保留、完整性校验 |

### 高可用

企业级可靠性保障。

| 能力 | 说明 |
|------|------|
| **集群高可用** | 自动故障转移、读写分离、滚动升级 |
| **分布式查询** | 跨分片聚合、分布式备份恢复 |
| **可观测性** | 慢查询分析、指标采集、告警 |

联系方式：license@ontovalue.com

---

## 许可证

OntoDB 采用双许可模式：

| 版本 | 许可证 | 说明 |
|------|--------|------|
| **社区版** | BUSL-1.1 | 免费使用，禁止提供 DBaaS 托管服务，2031-09-15 转 Apache-2.0 |
| **企业版** | 商业许可 | 全功能，需购买许可证 |

**社区版（BUSL-1.1）：**
- 允许：内部使用、本地部署、二次开发、非商业分发
- 允许：企业自用、SaaS 产品中嵌入使用
- 禁止：提供 OntoDB 作为云数据库服务（DBaaS）
- 2031-09-15 自动转为 Apache License 2.0
- 详见 [LICENSE](LICENSE)

**企业版（商业许可）：**
- 高级推理引擎（完整 DSL + OWL 集成 + 分布式推理 + 性能分析）
- 安全合规（RBAC + LDAP + SM4/AES 加密 + KMS + 审计保留）
- 高可用（集群故障转移 + 跨分片查询 + 备份恢复）
- 详见 [LICENSE.COMMERCIAL](LICENSE.COMMERCIAL)
- 联系方式：license@ontovalue.com

---

## 贡献

欢迎贡献！请查看 [CONTRIBUTING.md](CONTRIBUTING.md)。
